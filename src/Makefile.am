SUBDIRS = linux

DEFS += -DLOCALEDIR=\"$(localedir)\"

bin_PROGRAMS = fscd

AM_CPPFLAGS = $(HAL_CFLAGS) $(X11_CFLAGS) $(LIBWACOMCFG_CFLAGS) $(LIBXOSD_CFLAGS)

fscd_SOURCES = fscd.c
fscd_LDADD = $(HAL_LIBS) $(X11_LIBS) $(LIBWACOMCFG_LIBS) $(LIBXOSD_LIBS)

EXTRA_DIST = Xwacom.h
